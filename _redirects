# Cloudflare Pages redirects and rewrites

# Redirect www to non-www (if desired)
# https://www.pangool.com/* https://pangool.com/:splat 301

# SPA fallback - serve index.html for non-file requests
# Uncomment if you need SPA routing
# /* /index.html 200

# Custom 404 page (if you create one)
# /* /404.html 404

