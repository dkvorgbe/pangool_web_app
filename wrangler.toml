name = "pangool-web-app"
compatibility_date = "2024-10-01"
pages_build_output_dir = "."

[env.production]
name = "pangool-web-app"
routes = [
  { pattern = "pangool.com", custom_domain = true },
  { pattern = "www.pangool.com", custom_domain = true }
]

[env.staging]
name = "pangool-web-app-staging"

# Static file serving configuration
[site]
bucket = "."

# Custom build configuration (if needed)
# [build]
# command = ""
# cwd = ""
# watch_dir = ""

